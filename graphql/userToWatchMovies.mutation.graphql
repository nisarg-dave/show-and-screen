mutation RemoveFromToWatchMoviesMutation(
  $user: FindUserInput!
  $movie: MovieIdInput!
) {
  removeFromWantToWatchMovies(user: $user, movie: $movie) {
    id
    title
    posterPath
    backdropPath
  }
}
